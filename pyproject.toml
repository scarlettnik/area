[project]
name = "area-heat-routing"
version = "0.1.0"
description = "Validated heat network topology optimization for the LCT 2026 model"
requires-python = ">=3.11"
dependencies = ["numpy>=2.3,<3", "shapely>=2.1,<3"]
